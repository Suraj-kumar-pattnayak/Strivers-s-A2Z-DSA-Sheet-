class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
      #Set
      h= set()
      for num in nums:
        if num not in h:
          h.add(num)
        else:
          return True
      return False
        
